<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jonathangorman.flipper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/loro_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/loro_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:theme="
        <activity android:name=".activities.LanguageChoiceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CardScreenActivity" />
        <activity android:name=".activities.CategoryChoiceActivity" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".activities.LanguageChoiceActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.jonathangorman.flipper.activities.LanguageChoiceActivity" />
        </activity>
    </application>

</manifest>